import { ElemWithResizeEvents, Div, Span, Li, Td } from '.';

describe('Exports', () => {
  it('is truthy', () => {
    expect(ElemWithResizeEvents).toBeTruthy();
    expect(Div).toBeTruthy();
    expect(Span).toBeTruthy();
    expect(Li).toBeTruthy();
    expect(Td).toBeTruthy();
  });
});
